package com.socialapp.heyya.qb.command;

public class QBRejectCallCommand{
/*extends ServiceCommand{
	private QBVoiceCallHelper voiceCallHelper;
	public QBRejectCallCommand(Context context, QBVoiceCallHelper voiceCallHelper,String successAction,
			String failAction) {
		super(context, successAction, failAction);
		// TODO Auto-generated constructor stub
		this.voiceCallHelper = voiceCallHelper;
	}
	
	public static void start(Context context) {
        Intent intent = new Intent(QBServiceConsts.REJECT_CALL_ACTION, null, context, QBService.class);
        context.startService(intent);
	}

	@Override
	protected Bundle perform(Bundle extras) throws Exception {
		this.voiceCallHelper.reject();
		return extras;
	}
*/
}
