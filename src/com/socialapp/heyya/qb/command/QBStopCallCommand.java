package com.socialapp.heyya.qb.command;

public class QBStopCallCommand {
/*extends ServiceCommand{

	private QBVoiceCallHelper voiceCallHelper;
	static Context mContext;
	public QBStopCallCommand(Context context, QBVoiceCallHelper voiceCallHelper,String successAction,
			String failAction) {
		super(context, successAction, failAction);
		// TODO Auto-generated constructor stub
		this.voiceCallHelper = voiceCallHelper;
	}

	public static void start(Context context) {
        Intent intent = new Intent(QBServiceConsts.STOP_CALL_ACTION, null, context, QBService.class);
        context.startService(intent);
	}
	@Override
	protected Bundle perform(Bundle extras) throws Exception {
		this.voiceCallHelper.stopCall();
		return extras;
	}
	*/
}
